/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://tucasamuhas.com', // replace with your domain name!
    generateRobotsTxt: true, // (optional) automatically creates robots.txt
    sitemapSize: 7000, // (optional) splits large sitemaps
    changefreq: 'daily',
    priority: 0.7,
  };
  